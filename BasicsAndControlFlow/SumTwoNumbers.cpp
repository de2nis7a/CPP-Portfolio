//SumTwoNumbers.cpp

#include <iostream>
using namespace std;

//This program reads two natural numbers from the user and prints their sum.

int main() {
    int x, y;
    cout << "Enter two natural numbers: ";
    cin >> x >> y;
    cout << "The sum of the numbers is: " << x + y << endl;
    return 0;
}